services:
  claude-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: claude-dev
    volumes:
      - ..:/workspace
      - ~/.claude:/root/.claude
      - ~/.claude.json:/root/.claude.json
    network_mode: host
    environment:
      - TERM=xterm-256color
    stdin_open: true
    tty: true
